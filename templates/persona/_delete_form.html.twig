<form method="post" action="{{ path('persona_delete', {'id': persona.id}) }}" onsubmit="return confirm('¿Está seguro de que quiere eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ persona.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
