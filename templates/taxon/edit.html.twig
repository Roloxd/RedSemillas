{% extends 'base.html.twig' %}

{% block title %}Edit Taxon{% endblock %}

{% block body %}
    <h1>Edit Taxon</h1>

    {{ include('taxon/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('taxon_index') }}">back to list</a>

    {{ include('taxon/_delete_form.html.twig') }}
{% endblock %}
