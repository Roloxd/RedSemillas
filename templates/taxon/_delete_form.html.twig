<form method="post" action="{{ path('taxon_delete', {'id': taxon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taxon.id) }}">
    <button class="btn">Delete</button>
</form>
