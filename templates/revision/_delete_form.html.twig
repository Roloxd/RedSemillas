<form method="post" action="{{ path('revision_delete', {'id': revision.id}) }}" onsubmit="return confirm('¿Está seguro de que quiere eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ revision.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
