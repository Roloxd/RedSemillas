<form method="post" action="{{ path('imagen_seleccionada_delete', {'id': imagen_seleccionada.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ imagen_seleccionada.id) }}">
    <button class="btn">Delete</button>
</form>
