<form method="post" action="{{ path('germinacion_delete', {'id': germinacion.id}) }}" onsubmit="return confirm('¿Está seguro de que quiere eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ germinacion.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
