<form method="post" action="{{ path('germinacion_delete', {'id': germinacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ germinacion.id) }}">
    <button class="btn">Delete</button>
</form>
