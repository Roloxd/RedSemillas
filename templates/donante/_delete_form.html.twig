<form method="post" action="{{ path('donante_delete', {'id': donante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donante.id) }}">
    <button class="btn">Delete</button>
</form>
