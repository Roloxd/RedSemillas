<form method="post" action="{{ path('envase_delete', {'id': envase.id}) }}" onsubmit="return confirm('¿Está seguro de que quiere eliminar este elemento?');" class="float-right">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ envase.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
