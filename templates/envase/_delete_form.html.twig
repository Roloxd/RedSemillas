<form method="post" action="{{ path('envase_delete', {'id': envase.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ envase.id) }}">
    <button class="btn">Delete</button>
</form>
