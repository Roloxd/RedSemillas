<form method="post" action="{{ path('fitosanitario_delete', {'id': fitosanitario.id}) }}" onsubmit="return confirm('¿Está seguro de que quiere eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fitosanitario.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
