<form method="post" action="{{ path('metodo_empleado_delete', {'id': metodo_empleado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ metodo_empleado.id) }}">
    <button class="btn">Delete</button>
</form>
