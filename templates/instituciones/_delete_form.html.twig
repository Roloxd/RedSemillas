<form method="post" action="{{ path('instituciones_delete', {'INSTCODE': institucione.INSTCODE}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ institucione.INSTCODE) }}">
    <button class="btn">Delete</button>
</form>
