{% extends 'base.html.twig' %}

{% block title %}Mejoras{% endblock %}

{% block body %}
    <h1>Mejoras</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mejora.id }}</td>
            </tr>
            <tr>
                <th>MetodoMejora</th>
                <td>{{ mejora.metodoMejora }}</td>
            </tr>
            <tr>
                <th>DescripcionProcedimiento</th>
                <td>{{ mejora.descripcionProcedimiento }}</td>
            </tr>
            <tr>
                <th>ImagenesProceso</th>
                <td>{{ mejora.imagenesProceso }}</td>
            </tr>
            <tr>
                <th>Observaciones</th>
                <td>{{ mejora.observaciones }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mejoras_index') }}">back to list</a>

    <a href="{{ path('mejoras_edit', {'id': mejora.id}) }}">edit</a>

    {{ include('mejoras/_delete_form.html.twig') }}
{% endblock %}
