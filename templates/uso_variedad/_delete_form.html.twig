<form method="post" action="{{ path('uso_variedad_delete', {'id': uso_variedad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ uso_variedad.id) }}">
    <button class="btn">Delete</button>
</form>
