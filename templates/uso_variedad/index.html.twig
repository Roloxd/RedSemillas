{% extends 'base.html.twig' %}

{% block title %}UsoVariedad index{% endblock %}

{% block body %}
    <h1>UsoVariedad index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for uso_variedad in uso_variedads %}
            <tr>
                <td>{{ uso_variedad.id }}</td>
                <td>{{ uso_variedad.descripcion }}</td>
                <td>
                    <a href="{{ path('uso_variedad_show', {'id': uso_variedad.id}) }}">show</a>
                    <a href="{{ path('uso_variedad_edit', {'id': uso_variedad.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('uso_variedad_new') }}">Create new</a>
{% endblock %}
