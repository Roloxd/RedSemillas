<form method="post" action="{{ path('pago_delete', {'id': pago.id}) }}" onsubmit="return confirm('¿Está seguro de que quiere eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pago.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
