{% extends 'backend-base.html.twig' %}

{% block title %}Pago{% endblock %}

{% block stylesheets %}
      {{ include('be-layout/stylesheet.html', sandboxed = true) }}
{% endblock %}

{% block body %}
<div class="wrapper">
    {{ include('be-layout/preloader.html', sandboxed = true) }}
    {{ include('be-layout/navbar.html', sandboxed = true) }}
    {{ include('be-layout/sidebar.html', sandboxed = true) }}

    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        <div class="d-flex justify-content-between pt-3">
            <h1>Pago</h1>
            {{ include('pago/_delete_form.html.twig') }}
        </div>

        <table class="table">
            <tbody>
                <tr>
                    <th>ID</th>
                    <td>{{ pago.id }}</td>
                </tr>
                <tr>
                    <th>Fecha de renovación</th>
                    <td>{{ pago.fechaRenovacion|date('Y-m-d') }}</td>
                </tr>
                <tr>
                    <th>Fecha de pago</th>
                    <td>{{ pago.fechaPago|date('Y-m-d') }}</td>
                </tr>
                <tr>
                    <th>Persona</th>
                    <td>{{ pago.persona }}</td>
                </tr>
            </tbody>
        </table>

        <div class="d-flex justify-content-between">
            <a class="btn btn-secondary" href="{{ path('pago_index') }}">Volver a la lista</a>
            <a class="btn btn-primary" href="{{ path('pago_edit', {'id': pago.id}) }}">Editar</a>
        </div>
    </div>
    
    {{ include('be-layout/footer.html', sandboxed = true) }}

    <!-- Control Sidebar -->
    <aside class="control-sidebar control-sidebar-dark">
        <!-- Control sidebar content goes here -->
    </aside>
    <!-- /.control-sidebar -->
</div>
<!-- ./wrapper -->

{{ include('be-layout/js.html', sandboxed = true) }}
{% endblock %}

